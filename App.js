import React from 'react';
import {View, Text} from "react-native";

// 함수
const App = () => {

    // 함수, 상수, 변수, 상태

    // 실질적으로 화면에 보여주는 부분
    return (
        <View>
            <Text>
                Hi~
            </Text>

        </View>
    );
};

export default App;

//  styleSheet 선언

// 코딩기본법칙
// 1. 위에서 아래 순서로 빌드를 한다.
// 2. = 을 기준으로 오른쪽에서 왼쪽으로 치환한다.
// 3. . 하위메소드 호출
// 4. {} 직접호출
// 5. () 함수들의 모음